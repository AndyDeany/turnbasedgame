class Options(object):  # Make this a module? or even a dictionary?
    # Inventory
    show_item_info_at_side = False     #! Change this to load from the player's options file

    # Add a function to this class to make it load the player's settings from
    # a file of some sort. YAML if you don't care about hiding settings from users.
